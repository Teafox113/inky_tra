# 任務日誌與接續斷點

## 2026-09-12 — 建立獨立繁中編輯器工作區

- GitHub API 確認 Teafox113/inky_tra 的 fork=true，parent/source 均為 inkle/inky，預設分支 master。
- 複製完整上游歷史，起始 HEAD 77f7605e5f170354c26b50a601090175f6c0c800；建立 codex/zh-tw-workspace 分支，origin 指向使用者 fork，upstream 指向 inkle/inky。
- 從既有解包程式匯入 457 個編輯器檔案，未匯入 node_modules、創作者頭像、遊戲劇本或注入工具。八個主要來源檔已先與舊主程式 app.asar 比對一致。匯入來源與 SHA-256 manifest 存於忽略的 .local/import-manifest.json。
- 版本統一為 1.8.0-dev.1，加入制式 version.json、README、Change Log、授權附件與原創範例。
- 所有 24 個 Ink 檔案都有來源与雜湊允許清單：21 個上游 MIT 通用語法片段、兩個原創範例及一份原創開發預載測試副本。四個上游完整故事檔從目前發布樹移除；上游 Git 歷史仍正常保留。
- 原專案的遊戲原文、翻譯、素材及 API 設定沒有搬入；原資料夾未更動。
- npm 安裝完成，lockfile 保留；實際 Electron 30.0.4、inkjs 2.2.3、Ink 1.2.0。
- 新版與原版 userData 隔離；開發啟動固定用 .local/user-data/。

### 已驗證

- 7 項離線自動測試全部通過：版本同步、JS 語法、Ink 語法保護、詞彙表、中文示範編譯及遊玩路線、英文示範雙分支、發布規則負向測試。
- 發布範圍掃描通過；首次掃描攔到舊程式註解中的特定遊戲資料夾名稱，已改為通用說明。
- 使用獨立開發啟動程序與原創示範實際啟動 Inky，Windows 視窗顯示中文文本及預覽，問題列顯示「沒有問題」。未測試付費 API、未登入。
- 本機 dist/ 有 Windows 測試 ZIP 與解壓目錄；已另行讀取 asar 比對 version.json、package.json、main.js、translationView.js、assistantView.js，與工作區相同。
- 封裝共 24 個 Ink 檔，未知劇本 0，禁止檔案模式 0；EXE ProductName=Inky Translation Fork、CompanyName=FloofyFox，FileDescription 含完整開發版號。Windows 數值版本使用 1.8.0，開發版尾碼保留於說明與 app metadata。

### 建置注意

- 初次建置曾回報系統暫存路徑檔案不存在，後改用 .local/packager-temp。
- 隨後工具亦曾回報目錄 rename 的 EPERM，但目標 ZIP 與完整目錄已存在且另經實際讀取驗證；不能僅根據命令退出碼認定沒有產物。輸出搬移已改為複製以降低 Windows 目錄鎖定問題，新複製路徑仍待下一次新版本建置驗證。
- 建置禁止覆蓋同版既有輸出；需要重建時先備份舊輸出或更新版本。
- 本次沒有推送、建立 GitHub Release 或宣告正式發布。dist/ 為開發測試產物，完整 UI 驗收仍待完成。

### 後續優先項目

1. 初次啟動原生選單英文問題已於 1.8.0-dev.2 修復並以封裝 EXE 實測；繼續完成其他互動驗收。
2. 依繁中測試計畫驗證 IME、另存、搜尋取代、語言偏好、助理與 API；不要將目前啟動成功當成全面通過。
3. 核對圖示字型細項授權（Photon/Fontello、IcoMoon）並完成 Electron 升級评估，才進入公開發布驗收。
4. 頭像權利已於本日後續對話確認，見下節；圖片尚未嵌入程式。
5. 發布一律由此倉庫整理、檢查與推送，禁止把舊遊戲翻譯工作目錄加入 Git。

## 2026-09-12 — 共用制式資訊與頭像權利確認

- 使用者確認為共用創作者頭像的著作權持有人；更新共用 creator.json、README 與頭像權利紀錄，包含素材 SHA-256。
- 建立 `F:\APP開發\制式資訊\INKY`，保存版本、GitHub README、Change Log、授權及 README 引用的說明文件，供其他程式參考。
- 版本與 README 仍以本工作區為權威來源，共用資料夾是同步參考；附同步資料.ps1，更新前自動備份有變更的參考檔案。
- 本次只更新文件與頭像權利 metadata，不改動 UI、不嵌入圖片、不調整版號、不重打包或推送。既有 dist 測試包仍是先前快照，權利狀態文字尚未同步到包內。

## 2026-09-12 — 1.8.0-dev.2：繁中選單與範例入口修復

- 使用者回報 EXE 仍為英文且找不到範例。根因：原生選單鍵含 &，繁中詞庫不含；首次啟動未設定預設 UI 語言；範例只有封裝內檔案及插入片段入口。
- 已修正翻譯鍵正規化、預設繁中、語法片段延遲翻譯；明確英文偏好仍保留。
- 新增頂層範例選單與首次啟動示範；範例複製到各自 userData，重新開啟不覆蓋使用者修改。
- 新版 ZIP 附「範例」資料夾及「開始使用.txt」。版本、package/lock、README、Change Log 已同步 1.8.0-dev.2。
- 9 項離線測試全部通過，發布檢查通過；Windows 建置本次 exit 0 成功，先前改用複製的建置路徑已驗證。
- 實際啟動 dist 的 dev.2 EXE，以全新 .local/qa-visible-dev2 設定驗證：繁中原生選單、範例選單、雨天書店自動開啟、中文預覽與「沒有問題」狀態均正常；沒有登入或呼叫 API。
- 封裝主要程式與來源逐檔相符；外部示範與原創來源相同。舊 dev.1 保留供備份，請使用 dev.2。未推送 GitHub。

## 2026-09-12 — 1.8.0-dev.3：霧港十三夜預設教學

- 依使用者指示參考其 Novel Studio 範例的開頭與設定，新寫五檔 Ink 教學，未帶入第三方商業劇本。
- 包含姓名、三背景、5 點屬性、4 點技能、退點／重設、三層對話、物品、記憶、固定分數檢定與四結局。預覽仍以選項輸入；姓名可編輯 VAR；不宣稱有自由輸入框或自動玩家存檔。
- 新增 mist 範例入口與独立歡迎標記，使 dev.2 使用者升級後也展示一次，保留原範例與編輯副本。
- 已通過 15 項測試及 29 檔 Ink 發布檢查；角色分配與全部四結局以實際選項驗證。
- 下一步驗證 Windows 封裝啟動；尚未推送 GitHub。
- Windows dev.3 ZIP 建置成功；實際 EXE 在獨立 QA 設定下顯示繁中選單、五檔案與預覽，狀態「沒有問題」。舊歡迎標記不阻止新範例展示；12 個封裝關鍵檔案及 6 個範例副本逐一相符。使用者已在視窗中操作角色建立，未繼續插入自動點擊。

## 2026-09-12 — GitHub 原始碼推送

- 使用者明確授權推送至 Teafox113/inky_tra；目標為既有預設 master 分支，保留上游歷史，不使用強制推送。
- 提交範圍為繁中編輯器、教學範例、測試、建置與授權文件。dist、.local、私人設定與第三方遊戲內容不提交。
- 這次推送開發版原始碼；不建立正式 Release，尚未完成的互動驗收與字型授權核對仍保留於文件。
